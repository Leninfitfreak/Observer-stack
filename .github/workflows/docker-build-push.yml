name: Docker Build and Push

on:
  push:
    branches:
      - dev
  workflow_dispatch:

env:
  GHCR_IMAGE: ghcr.io/${{ github.repository_owner }}/ai-observer-agent
  INFRA_REPO: Leninfitfreak/leninkart-infra
  INFRA_BRANCH: dev
  INFRA_BACKEND_DEPLOYMENT_FILE: platform/ai-observer/base/deployment.yaml
  INFRA_OBSERVER_AGENT_DEPLOYMENT_FILE: k8s/observer-agent/deployment.yaml

jobs:
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve CI variables
        id: vars
        shell: bash
        env:
          DOCKERHUB_USERNAME_IN: ${{ secrets.DOCKERHUB_USERNAME || vars.DOCKERHUB_USERNAME }}
        run: |
          if [ -z "${DOCKERHUB_USERNAME_IN}" ]; then
            echo "DOCKERHUB_USERNAME is missing. Set repo variable DOCKERHUB_USERNAME or secret DOCKERHUB_USERNAME."
            exit 1
          fi
          echo "dockerhub_username=${DOCKERHUB_USERNAME_IN}" >> "${GITHUB_OUTPUT}"
          echo "dockerhub_image=docker.io/${DOCKERHUB_USERNAME_IN}/ai-observer-agent" >> "${GITHUB_OUTPUT}"
          echo "dockerhub_observer_agent_image=docker.io/${DOCKERHUB_USERNAME_IN}/observer-agent" >> "${GITHUB_OUTPUT}"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ steps.vars.outputs.dockerhub_username }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.GHCR_IMAGE }}
            ${{ steps.vars.outputs.dockerhub_image }}
          tags: |
            type=raw,value=dev
            type=sha

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Extract metadata (observer-agent)
        id: meta_observer_agent
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ steps.vars.outputs.dockerhub_observer_agent_image }}
          tags: |
            type=raw,value=dev
            type=sha

      - name: Build and push observer-agent image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.observer-agent
          push: true
          tags: ${{ steps.meta_observer_agent.outputs.tags }}
          labels: ${{ steps.meta_observer_agent.outputs.labels }}

      - name: Checkout infra repo
        uses: actions/checkout@v4
        with:
          repository: ${{ env.INFRA_REPO }}
          ref: ${{ env.INFRA_BRANCH }}
          token: ${{ secrets.INFRA_REPO_PAT }}
          path: infra-repo

      - name: Update infra image tags
        shell: bash
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          BACKEND_IMAGE="${{ steps.vars.outputs.dockerhub_image }}:sha-${SHORT_SHA}"
          OBSERVER_AGENT_IMAGE="${{ steps.vars.outputs.dockerhub_observer_agent_image }}:sha-${SHORT_SHA}"
          BACKEND_FILE="infra-repo/${{ env.INFRA_BACKEND_DEPLOYMENT_FILE }}"
          OBSERVER_AGENT_FILE="infra-repo/${{ env.INFRA_OBSERVER_AGENT_DEPLOYMENT_FILE }}"
          sed -i "s|image: .*ai-observer-agent.*|image: ${BACKEND_IMAGE}|g" "${BACKEND_FILE}"
          sed -i "s|image: .*observer-agent.*|image: ${OBSERVER_AGENT_IMAGE}|g" "${OBSERVER_AGENT_FILE}"
          echo "Updated ${BACKEND_FILE} to ${BACKEND_IMAGE}"
          echo "Updated ${OBSERVER_AGENT_FILE} to ${OBSERVER_AGENT_IMAGE}"

      - name: Commit and push infra update
        shell: bash
        run: |
          cd infra-repo
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "${{ env.INFRA_BACKEND_DEPLOYMENT_FILE }}" "${{ env.INFRA_OBSERVER_AGENT_DEPLOYMENT_FILE }}"
          if git diff --cached --quiet; then
            echo "No infra manifest changes to commit."
            exit 0
          fi
          git commit -m "chore(images): bump ai-observer and observer-agent to ${GITHUB_SHA::7}"
          git push origin "${{ env.INFRA_BRANCH }}"
