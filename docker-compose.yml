services:
  backend:
    build:
      context: .
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DB_ECHO_SQL: ${DB_ECHO_SQL}
      AGENT_TOKEN: ${AGENT_TOKEN}
      CLUSTER_ID: ${CLUSTER_ID}
      DEFAULT_NAMESPACE: ${DEFAULT_NAMESPACE}
      DEFAULT_SERVICE: ${DEFAULT_SERVICE}
      DEFAULT_SEVERITY: ${DEFAULT_SEVERITY}
      DEFAULT_WINDOW_MINUTES: ${DEFAULT_WINDOW_MINUTES}
      PROMETHEUS_URL: ${PROMETHEUS_URL}
      LOKI_URL: ${LOKI_URL}
      JAEGER_URL: ${JAEGER_URL}
      KUBERNETES_ENABLED: ${KUBERNETES_ENABLED}
      KUBERNETES_NAMESPACE: ${KUBERNETES_NAMESPACE}
      OBS_DISCOVERY_ENABLED: ${OBS_DISCOVERY_ENABLED}
      DISCOVERY_NAMESPACES: ${DISCOVERY_NAMESPACES}
      K8S_API_URL: ${K8S_API_URL}
      K8S_VERIFY_SSL: ${K8S_VERIFY_SSL}
      OBS_DISCOVERY_REFRESH_SECONDS: ${OBS_DISCOVERY_REFRESH_SECONDS}
      OBS_DISCOVERY_VALIDATION_TIMEOUT_SECONDS: ${OBS_DISCOVERY_VALIDATION_TIMEOUT_SECONDS}
      LLM_PROVIDER: ${LLM_PROVIDER}
      LLM_MODEL: ${LLM_MODEL}
      LLAMA_API_URL: ${LLAMA_API_URL}
      LLAMA_API_KEY: ${LLAMA_API_KEY}
      LLAMA_MODEL: ${LLAMA_MODEL}
      OLLAMA_URL: ${OLLAMA_URL}
      OLLAMA_API_KEY: ${OLLAMA_API_KEY}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      HTTP_TIMEOUT_SECONDS: ${HTTP_TIMEOUT_SECONDS}
      HTTP_ATTEMPTS: ${HTTP_ATTEMPTS}
      INCIDENT_DETECTION_ENABLED: ${INCIDENT_DETECTION_ENABLED:-true}
      INCIDENT_DETECTION_INTERVAL: ${INCIDENT_DETECTION_INTERVAL:-30}
      ANOMALY_THRESHOLD: ${ANOMALY_THRESHOLD:-0.65}
      SERVICE_DISCOVERY_MODE: ${SERVICE_DISCOVERY_MODE:-auto}
    ports:
      - "8080:8080"
    networks:
      default:
      minikube:
        ipv4_address: 192.168.49.10
    depends_on:
      - postgres

  observer-agent:
    profiles: ["local-agent"]
    build:
      context: .
      dockerfile: Dockerfile.observer-agent
    environment:
      CLUSTER_ID: ${AGENT_CLUSTER_ID}
      CENTRAL_URL: ${AGENT_CENTRAL_URL}
      AGENT_TOKEN: ${AGENT_TOKEN}
      PROM_URL: ${AGENT_PROM_URL}
      PUSH_INTERVAL: ${AGENT_PUSH_INTERVAL}
      ENVIRONMENT: ${AGENT_ENVIRONMENT}
    depends_on:
      - backend

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - ai-observer-pgdata:/var/lib/postgresql/data

volumes:
  ai-observer-pgdata:

networks:
  minikube:
    external: true
    name: minikube
